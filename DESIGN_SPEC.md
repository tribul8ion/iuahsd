# MedReminder — функционал и наполнение страниц (дизайн-бриф)

## 0. О приложении

- **Что это**: персональный трекер приёма лекарств, привычек, задач и визитов к врачу. Работает как **Telegram Mini App**, мобильный формат (~390px), тёмная тема, язык интерфейса RU/EN.
- **Фишка домена**: все данные шифруются «кодовым словом» пользователя на устройстве (сервер его не видит). Поэтому есть экраны создания/ввода кодового слова и акцент на приватность.
- **Геймификация**: виртуальный питомец-растение (Фикус), опыт XP, уровни, достижения, стрики по привычкам. Питомец никогда не умирает — просто скучает.
- **Тон**: дружелюбный, обращение на «ты».
- **Дизайн-система**: Material Design 3, тёмная тема. Фон `#131313`, карточки `#1C1C1E`, строки/степ-поверхности `#242425`, инлеи/чипы/треки `#2C2C2E` → `#353534`. Акцент — бледный лайм `#F9FFD0` (метрики, прогресс, активные состояния, CTA с тёмным текстом `#2C3400`), вторичный лайм `#B8D220`, оранжевый `#F26D21` (стрики), ошибка/риск `#FFB4AB`. Текст `#E5E2E1`, вторичный `#C6C9AE`. Радиус карточек 24px, круглые пилюли для чипов/навигации, радиус кнопок 22–24px. Шрифт Hanken Grotesk. Капсовые лейблы секций 12px, letter-spacing 0.2em, по-русски тоже капсом.
- **Навигация**: нижняя плавающая пилюля — 5 слотов: СЕГОДНЯ · КАЛЕНДАРЬ · [ большая круглая FAB «+» по центру ] · ПРИВЫЧКИ · ЕЩЁ. Активный пункт — лаймовый, неактивные — серые на 50%.

---

## 1. Сущности и их атрибуты (наполнение карточек)

| Сущность | Атрибуты в UI |
|---|---|
| **Лекарство (med)** | название · доза (число + ед.: мг/табл/мл/капли…) · расписание (Утро/День/Вечер/своё время) · частота (каждый день / дни недели / раз в N дней) · цвет-метка (6 цветов) · свитч активности · колокольчик уведомлений |
| **Привычка (habit)** | название · эмодзи-тег (☕ вода, спорт, сон, книги, без курения, медитация, еда) · время · частота · текущий стрик (дней подряд) · свитч/колокольчик |
| **Задача (once/task)** | название · дата · время · заметка · напоминания заранее (до 3 чипов: 30 мин, 1 ч, 3 ч, 12 ч, за день, за 2 дня) · проект |
| **Приём врача (doc)** | как задача · дефолтные напоминания «за день и за 3 часа» |
| **Заметка (note)** | просто текст, зашифрован целиком · проект |
| **Отметка дня (mark)** | инстанс записи на конкретный день: время · статус (принято ✅ / нет ○) · подпись расписания |
| **Проект** | название · список задач и заметок · можно удалять (задачи остаются) |
| **Питомец** | эмодзи по уровню (🌱→🌿→🌳…), уровень, XP до следующего уровня, настроение (ждёт / подбодрен / цветёт) |
| **Достижение** | эмодзи, название капсом, условие, редкость, «лестница» ступеней по тегам привычек |

Цвета-метки лекарств: blue `#3B82F6`, cyan `#06B6D4`, pink `#EC4899`, red `#EF4444`, indigo `#6366F1`, lime `#84CC16`. Иконки слотов дня: восход (утро, янтарь), солнце (день, оранж), луна (вечер, фиолетовый) — глиф цветной на тёмном круге `#2C2C2E`.

---

## 2. Экраны входа (поток до приложения)

1. **Consent / Welcome**: заголовок «Добро пожаловать в MedReminder», три буллета с иконками (шифрование, приватность, удаление данных), кнопка «Принять и продолжить», ссылка «Политику конфиденциальности».
2. **Onboarding (создание кодового слова)**: лаймовая плитка с иконкой ключа, заголовок «Придумайте кодовое слово», подзаголовок про шифрование, 2 поля (пароль + повтор), чекбокс-подтверждение «код восстановить невозможно», CTA «Создать и зашифровать», состояние загрузки «Создаём ключ шифрования…», ошибки (короткое/простое/не совпадает).
3. **Unlock**: то же визуально, одно поле, CTA «Разблокировать», ошибка «Неверное кодовое слово».
4. **Auth error**: экран ошибки авторизации Telegram с кнопкой «Повторить».

---

## 3. Главная — «Сегодня» (`/`)

**Шапка (плоская)**: капс-подпись MEDREMINDER (лайм), заголовок СЕГОДНЯ, подпись-дата капсом («ПЯТНИЦА, 18 СЕНТЯБРЯ»); справа чип питомца (эмодзи + его настроение, тап → страница питомца).

**Блоки сверху вниз:**

1. **Hero-карточка прогресса** (`#1C1C1E`, r24): слева капс-лейбл «ПРОГРЕСС ДНЯ» → гигантская метрика `3/6` (44–48px лайм) → строка «3 из 6 принято» → лаймовый статус капсом («ВСЁ СДЕЛАНО НА СЕГОДНЯ!» / «ПРОДОЛЖАЙ, ПОЧТИ ВСЁ!»); справа кольцевой гейдж: трек `#2C2C2E`, дуга лайм, в центре `%`.
2. **Две stat-карточки в ряд** (r24): минимум высоты ~108px.
   - «ЛУЧШИЙ СТРИК» + иконка пламени лайм → крупное число.
   - «НА ЭТОЙ НЕДЕЛЕ» + моно-значение `19 из 22` → прогресс-бар: трек `#2C2C2E`, заполнение лайм.
3. **Лента дня по времени**: группы с чипом-временем (тёмная пилюля `#2C2C2E`, капс, напр. `07:30`), под ней карточки-строки (r24, паддинг 16, высота ~72):
   - тёмный круг с цветным глифом (слот дня для лекарств / эмодзи-тег для привычек / иконки задача/врач),
   - название капсом 13px + цветная точка-метка лекарства,
   - под-строка «07:30 · Каждый день» серой,
   - справа: пилюля стрика (огонёк + число, оранжевый тинт) + круглый чек-бокс: пустой тёмный ○ → нажатие → лаймовый с тёмной галочкой (и хаптика). Тап по заполненному — снять отметку.
4. **Состояния**: все отмечены → конфетти + поздравление; пустой день → EmptyState (иконка на тональной плитке, «На сегодня ничего нет» + подсказка про кнопку «+»); загрузка → спиннер лайм; ошибка → центр с иконкой и текстом.

---

## 4. «Календарь» (`/calendar`)

- Шапка: КАЛЕНДАРЬ + подпись «N отметок в сентябре».
- **Карточка месяца**: стрелки ‹ › (тёмные круги), название месяца, строка дней недели капсом, сетка дат: числа; под числами цветные точки по типам записей (зелёная=лекарства, оранжевая=привычки, фиолетовая=задачи, розовая=приёмы); сегодня — лаймовый круг с тёмной цифрой; выбранный день — лаймовый обводок. Под сеткой — легенда из 4 цветных точек с подписями.
- **Агенда выбранного дня**: подпись «принято X из Y» (серой капсой), список тeх же строк-отметок (круг-глиф, имя капсом, время, чек-бокс работает и тут — можно отмечать задним числом; подсказка «Отметка задним числом пересчитает стрик»); врач/задача под строкой — фраза напоминаний «за 1 час и за день».
- Пустой день → «На этот день ничего не запланировано».

## 5. «Лекарства» (`/medications`)

Шапка + CTA-кнопка «Добавить лекарство» (лайм, r22). Список строк: тёмный круг со слот-иконкой (утро/день/вечер), название капсом, строка «Схема · 08:30 · 25 мг», цветная точка-метка; справа колокольчик (вкл/выкл/заглушен-до-завтра — янтарный), свитч активности, иконки редактировать/удалить. Неактивные записи — 55% opacity. Пусто → «Вы ещё не добавили ни одного лекарства».

## 6. «Привычки» (`/habits`)

Аналогично: CTA «Добавить привычку», строки: тёмный круг с эмодзи-тегом, название капсом, «07:30 · Каждый день», пилюля стрика 🔥15, колокольчик, свитч. Тап по строке → редактирование.

## 7. «Планы и заметки» (`/plans`)

- Шапка «ПЛАНЫ» + подпись «N записей».
- **Поиск** — тёмная строка с иконкой (локальный, по расшифрованным данным).
- Секция капс-лейбл «ПРОЕКТЫ» + маленькая лаймовая круглая кнопка «+»: карточки проектов (название, счётчик задач, чемодан/папка-глиф). Тап → шторка проекта: заголовок, действия «Переименовать»/«Удалить» (красный тинт), список задач проекта теми же строками, CTA «Добавить задачу».
- Секция «ЗАМЕТКИ»: строки-заметки (иконка 📝 на тёмном круге, обрезанный текст, проект-чип). Тап → редактор заметки (textarea, счётчик символов, выбор проекта, ошибки лимитов — янтарь/красный).
- Пустые состояния: «Проектов пока нет», «Заметок пока нет», «Ничего не найдено».

## 8. «Питомец» (`/game`)

- Шапка ПИТОМЕЦ; **сегмент-контроль** pill: [Питомец | Полка] — активный сегмент лаймовый с тёмным текстом.
- **Сегмент «Питомец»**: сцена — большой круг с лаймовым свечением и эмодзи растения (96px), имя+уровень («Фикус · уровень 3»), настроение, XP-бар: трек `#2C2C2E`, заполнение лайм с мягким свечением + подпись «12 xp / до 4 — 8». Объясняющая карточка «Как он живёт» (растёт от отметок, никогда не умирает).
- **Сегмент «Полка»**: **полка достижений** — сетка карточек-бейджей (эмодзи крупно, название капсом, условие; полученные — на лаймовом тинте с полной opacity, неполученные — 55%); toast «Новое достижение!» с эмодзи и кнопкой «Красота» + конфетти.
- **Лестница тегов**: карточка на каждый тег привычек: эмодзи, название тега, ступени N/N (плитки-ступени: пройденные лаймовые), строка прогресса «3/7 — до «Кофейный барон» 4 дн.»; «Все ступени открыты!»

## 9. «Профиль» (`/profile`)

- Шапка: аватар-круг с инициалами (тёмный, лаймовая обводка) + капс-лейбл ПРОФИЛЬ + имя капсом + «с нами с 12.03.2026».
- **Три stat-плитки в ряд**: Записей / Достижения / Лучший стрик (значения лаймом).
- Секции-группы строк (заголовки капсом 11px, строки: тёмная плитка-иконка + название + chevron/значение):
  - «Язык» (выбор RU/EN списком с галочкой),
  - **Напоминания**: свитч «Напоминания включены/отключены», «Уведомления на сегодня» (статус + кнопка «Отключить на сегодня»), «Интервал повтора — N минут» (чипы-варианты + «Своё…» → модалка с цифровым колесом времени: поля ЧЧ:ММ, активное поле подсвечено лайм-кольцом),
  - **Безопасность**: «Сменить кодовое слово» (модалка: предупреждение янтарным тинтом, поля старое/новое/повтор, подсказка о минимуме), «Экспорт данных» (модалка: предупреждение, CTA выгрузки),
  - **Опасная зона**: «Удалить аккаунт» — красная строка, шторка подтверждения (иконка корзины на красном тинте, текст про безвозвратность, кнопки «Отмена» тональная / «Удалить» красная).

## 10. «Ещё» (шторка из навигации)

Список строк-переходов: Профиль, Лекарства, Планы и заметки, Достижения, (Админ-панель — только у админов). Строка = тёмная плитка-круг с иконкой + название + chevron.

## 11. FAB «+» → шторка «Что добавим?»

5 строк с эмодзи на тёмных кругах: **Лекарство** (доза и расписание), **Привычка** (дни недели и время), **Задача** (разовая, с дедлайном), **Приём врача** (напомню за день и за 3 часа), **Заметка** (зашифрованный текст). Тап → форма-шторка соответствующего типа.

**Формы** (bottom-sheet r24 сверху, заголовок + ✕): поля на тёмных инлеях, сегменты-чипы выбора (расписание Утро/День/Вечер/Своё; частота Каждый день/Дни недели/Раз в N дней; чипы дней недели Пн..Вс — выбранные лаймовые с тёмным текстом), выбор цвета (6 кружков, выбранный — лайм-кольцо), выбор тега эмодзи (сетка), доза (число + модалка единиц), чипы напоминаний, поле даты/времени, выбор проекта. Внизу CTA на всю ширину: лайм, тёмный текст, r22 («Создать и зашифровать»/«Сохранить изменения»). Валидация — красные подписи под полями.

Все шторки: тёмный лист `#1C1C1E` поверх затемнения 60%, сверху графин-handle, радиус 24, закрытие свайпом/тапом по фону.

## 12. Общие компоненты и состояния

- **Кнопки**: primary — лайм `#F9FFD0`, текст `#2C3400`, r22, h48–54; tonal/cancel — `#242425`; danger — красный фон `#E11D48` белым (или красный текст на тинте).
- **Свитч**: трек лайм, knob угольный `#1C1C1E`; выкл — трек серый 18%, knob светлый.
- **Чип**: пилюля `#2C2C2E`, капс 10–12px.
- **Spine/прогресс-бары**: трек `#2C2C2E`, заполнение лайм.
- **EmptyState**: тональная плитка 80px с иконкой, заголовок, подпись.
- **Toast-достижение** и конфетти; **хаптика** Telegram на отметках.
- Состояния записей: обычная / принятая (лайм-галочка) / неактивная (55%) / повреждённая (серое имя «Повреждённая запись»).

## 13. Админ-панель (`/admin`, опционально)

Metрики платформы: регистрации по неделям, % выполненных отметок, среднее лекарств/польз., активные за 7д, топ лекарств (нумерованный список). Под тёмной админской группировкой, не приоритет для редизайна.
